import { Dropdown, Menu, Card, Tooltip, Divider, Input, Form, Button } from "ant-design-vue"

export default [Dropdown, Menu, Menu.Item, Card, Tooltip, Divider, Input, Input.TextArea, Form, Form.Item, Button]
